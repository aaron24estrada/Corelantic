# Corelantic API configuration. Copy to .env and fill in real values.
# Real secrets never get committed — only this .env.example is tracked (see ../../SECURITY.md).

# Server
CORELANTIC_API_HOST=127.0.0.1
CORELANTIC_API_PORT=8080

# Shared secret the web BFF presents on every request. Generate with: openssl rand -hex 32
CORELANTIC_API_INTERNAL_API_KEY=

# Provider selection.
# DATA_SOURCE: `fixture` serves seeded, KRW-ballpark demo data with no external dependency
# (great for local dev before the Azure SQL replica lands); `azure_sql` is the real read
# replica (pending docs O-1, not implemented yet).
CORELANTIC_API_DATA_SOURCE=fixture
CORELANTIC_API_LLM_PROVIDER=claude

# Read-only connection to the source database. Leave blank until provisioned (docs O-1).
CORELANTIC_API_DATABASE_URL=

# Azure SQL (read-only, Entra auth). Server/database are required when DATA_SOURCE=azure_sql.
CORELANTIC_API_AZURE_SQL_SERVER=krw-platform-sql.database.windows.net
CORELANTIC_API_AZURE_SQL_DATABASE=krw-platform
# `device_code` (interactive dev, MFA) or `service_principal` (headless/prod).
CORELANTIC_API_AZURE_SQL_AUTH_MODE=device_code
# Only for service_principal. Leave blank for device_code.
CORELANTIC_API_AZURE_SQL_TENANT_ID=
CORELANTIC_API_AZURE_SQL_CLIENT_ID=
CORELANTIC_API_AZURE_SQL_CLIENT_SECRET=

# Schemas an entity source may name (comma-separated). Bounds the registry to vetted objects.
CORELANTIC_API_ALLOWED_SOURCE_SCHEMAS=gold_tspot

# LLM credentials. Leave blank until provisioned.
CORELANTIC_API_ANTHROPIC_API_KEY=
